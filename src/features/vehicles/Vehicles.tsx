import { useGetVehiclesQuery } from 'data/vehicles';
import * as React from 'react';

export const Vehicles: React.FC = () => {
  // const { data } = useGetVehiclesQuery();

  return <div>Vehicles</div>;
};
